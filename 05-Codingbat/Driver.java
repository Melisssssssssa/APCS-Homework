public class Driver{
    public String nonStart(String s, String p){
	return s.substring(1) +b.substring(1);
    }
    public int diff21(int n){
	if (n > 21){
	    return 2 * Math.abs(21 - n);
	}
	else {
	    return Math.abs(21 - n);
	}
    }

    public String makeAbba(String s, String p){
	return s + p + p + s;
    }
}
